{
  "name": "bitso-cli",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "bun build src/index.ts --compile --outfile dist/bitso",
    "dev": "bun run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "smol-toml": "^1.3.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.12.0"
  }
}
